//heroku

function defineCity(city) {
    if (city) {
        city = city;
    } else {
        city = Berlin;
    }
}

// short version

// function defineCity(city) {
//     city = city || "Berlin";
// }

//.........
